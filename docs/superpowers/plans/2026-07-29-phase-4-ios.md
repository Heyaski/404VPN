# 404VPN — Фаза 4: iOS-приложение

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Приложение, где пользователь вводит код доступа и подключается к VPN одной кнопкой. Никаких профилей и файлов конфигурации — только код.

**Architecture:** Два таргета. Приложение (SwiftUI) активирует код, хранит токен устройства в Keychain, запрашивает туннель у бэкенда и управляет подключением через `NETunnelProviderManager`. Расширение (`PacketTunnelProvider`) поднимает WireGuard через **WireGuardKit**. Конфиг передаётся расширению как строка wg-quick в `providerConfiguration` — так же устроено официальное приложение WireGuard.

Проект описывается **XcodeGen** (`project.yml` → `.xcodeproj`): добавить второй таргет и SPM-зависимость правкой `.pbxproj` вручную ненадёжно, а сгенерированный проект воспроизводим и читается в диффах.

**Tech Stack:** Swift 5.9+, SwiftUI, NetworkExtension, WireGuardKit (SPM, `wireguard-apple`), XcodeGen. Минимальная iOS 16.

## Global Constraints

- Дизайн строго по `docs/DESIGN.md`: фон `#070B14`, акцент `#34D399` со свечением, Inter/SF Pro 800 для крупных чисел, моноширинный шрифт для кода и адресов, радиус 8, фоновая сетка. Только тёмная тема.
- Токен устройства — **только в Keychain** (`kSecAttrAccessibleAfterFirstUnlock`), никогда в UserDefaults.
- Приватный ключ WireGuard не хранится в приложении: конфиг запрашивается у бэкенда и кладётся в `providerConfiguration` менеджера туннеля (защищённое системное хранилище).
- **Никаких упоминаний оплаты в интерфейсе** — ни кнопок, ни ссылок на бота (App Store Guideline 3.1.1). Только поле ввода кода и статус баланса.
- Базовый URL API — из `Info.plist` (`APIBaseURL`), чтобы не хардкодить домен в коде.
- Тексты русские, технический тон, без эмодзи.

---

### Task 1: Генерируемый проект (XcodeGen) с двумя таргетами

**Files:** Create `vpn_ios/project.yml`, `vpn_ios/App/Info.plist`, `vpn_ios/App/App.entitlements`, `vpn_ios/Tunnel/Info.plist`, `vpn_ios/Tunnel/Tunnel.entitlements`; переносится содержимое `vpn_ios/vpn_ios/*`

Таргеты: `404VPN` (приложение) и `404VPNTunnel` (Network Extension, `com.apple.networkextension.packet-tunnel`). Идентификаторы: `co.404studio.vpn` и `co.404studio.vpn.tunnel`. Группа приложений `group.co.404studio.vpn` — общая для приложения и расширения.

- [ ] **Step 1:** `brew install xcodegen`; написать `project.yml` со SPM-зависимостью `https://github.com/WireGuard/wireguard-apple` (продукт `WireGuardKit`).
- [ ] **Step 2:** `xcodegen generate` в `vpn_ios/`, затем `xcodebuild -scheme 404VPN -destination 'generic/platform=iOS Simulator' build` — сборка проходит.
- [ ] **Step 3:** commit.

### Task 2: Слой сети и хранение токена

**Files:** Create `vpn_ios/App/Api.swift`, `vpn_ios/App/Keychain.swift`

**Interfaces:**
- `struct TunnelConfig: Decodable` — зеркало ответа `/api/device/tunnel` (`privateKey`, `address`, `dns`, `peer`).
- `func wgQuickConfig() -> String` на `TunnelConfig` — собирает текст `[Interface]/[Peer]` для WireGuardKit.
- `enum ApiError: Error { case invalidCode, alreadyUsed, expired, revoked, suspended, unavailable, network(String) }`
- `actor Api { func redeem(code:) async throws -> RedeemResponse; func me() async throws -> Me; func tunnel() async throws -> TunnelConfig }`
- `enum Keychain { static func saveToken(_:); static func token() -> String?; static func clear() }`

- [ ] **Step 1:** тесты (XCTest, без сети): `wgQuickConfig()` из фикстуры ответа даёт корректный wg-quick; разбор кодов ошибок из JSON в `ApiError`; нормализация введённого кода.
- [ ] **Step 2:** реализация. **Step 3:** `xcodebuild test` зелёный. **Step 4:** commit.

### Task 3: Менеджер VPN

**Files:** Create `vpn_ios/App/VPNManager.swift`

**Interfaces:**
- `@MainActor final class VPNManager: ObservableObject` — `@Published status: NEVPNStatus`, `func install(config: TunnelConfig) async throws`, `func connect() async throws`, `func disconnect()`.
- Установка: `NETunnelProviderManager.loadAllFromPreferences` → берём существующий или создаём → `NETunnelProviderProtocol` c `providerBundleIdentifier`, `serverAddress` = endpoint, `providerConfiguration = ["wgQuickConfig": config.wgQuickConfig()]` → `saveToPreferences`.
- Статус — через `NEVPNStatusDidChange`.

- [ ] **Step 1:** реализация (юнит-тесты невозможны — системное API; проверяется на устройстве).
- [ ] **Step 2:** сборка. **Step 3:** commit.

### Task 4: Расширение туннеля

**Files:** Create `vpn_ios/Tunnel/PacketTunnelProvider.swift`

`startTunnel` читает `wgQuickConfig` из `providerConfiguration`, парсит `TunnelConfiguration(fromWgQuickConfig:name:)`, поднимает `WireGuardAdapter`. `stopTunnel` — останавливает адаптер. Логи адаптера — в `NSLog` (для отладки на устройстве).

- [ ] **Step 1:** реализация. **Step 2:** сборка обоих таргетов. **Step 3:** commit.

### Task 5: Дизайн-система и экран ввода кода

**Files:** Create `vpn_ios/App/Theme.swift`, `vpn_ios/App/RedeemView.swift`

`Theme` — цвета и шрифты из `docs/DESIGN.md`, модификатор фоновой сетки, стиль основной кнопки со свечением.

`RedeemView`: заголовок «404VPN» (404 акцентом), моноширинное поле ввода с автоформатированием `XXXX-XXXX-XXXX-XXXX` (верхний регистр, дефисы сами), кнопка «Активировать» (неактивна, пока не введено 16 символов), понятные сообщения об ошибках. Никаких упоминаний оплаты.

- [ ] **Step 1:** тест форматирования кода (чистая функция). **Step 2:** реализация. **Step 3:** сборка + скриншот в симуляторе. **Step 4:** commit.

### Task 6: Главный экран

**Files:** Create `vpn_ios/App/HomeView.swift`, `vpn_ios/App/AppState.swift`; Modify `vpn_ios/App/App.swift`

`HomeView`: круглая кнопка подключения с зелёным свечением в состоянии «подключено», статус моноширинными капсами (`ПОДКЛЮЧЕНО` / `ОТКЛЮЧЕНО` / `ПОДКЛЮЧЕНИЕ…`), баланс и остаток дней, предупреждение при `suspended`. `AppState` решает, что показать: нет токена → `RedeemView`, есть → `HomeView`.

- [ ] **Step 1:** реализация. **Step 2:** сборка + скриншоты обоих экранов. **Step 3:** commit.

### Task 7: Подготовка к запуску на устройстве

**Files:** Modify `docs/DEPLOY.md` (раздел iOS)

Что делает владелец: Apple Developer Program, App ID `co.404studio.vpn` и `co.404studio.vpn.tunnel` с capability Network Extensions (Packet Tunnel), App Group `group.co.404studio.vpn`, профили. В Xcode — выбрать Team, собрать на устройство.

- [ ] **Step 1:** дописать инструкцию. **Step 2:** commit.

---

## Definition of Done (Фаза 4)

- `xcodegen generate && xcodebuild build` собирает оба таргета без ошибок.
- Экраны ввода кода и подключения сняты скриншотами в симуляторе и соответствуют `docs/DESIGN.md`.
- На реальном устройстве (после подписи владельцем): ввод кода → активация → кнопка подключения → системный запрос на добавление VPN → статус «подключено», трафик идёт через NL-сервер.

## Ограничения

VPN не работает в симуляторе: `NEPacketTunnelProvider` там не поднимает реальный туннель. Экраны и логика проверяются в симуляторе, само подключение — только на устройстве с подписью от Apple Developer Program.
